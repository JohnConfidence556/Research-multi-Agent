services:
  - type: web
    name: aria-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:server --host 0.0.0.0 --port 8000
    envVars:
      - key: GROQ_API_KEY
      - key: TAVILY_API_KEY
        sync: false